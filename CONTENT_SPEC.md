# LLM 介绍文档 — 内容规格文档

> **版本**：v0.2  
> **日期**：2026-08-12  
> **状态**：撰写中  
> **用途**：定义每个章节文件的详细内容结构，经用户审核后作为撰写依据

---

## 撰写总则

1. 每章以 `# 第 N 章 标题` 开头，紧跟 `## 本章导引`（blockquote），末尾 `## 本章小结`
2. 正文用 `## N.x` 二级标题分节，`### N.x.y` 三级标题细分
3. 客观严谨教科书风格，类比仅作辅助
4. 每个公式后必须有文字解释所有符号
5. Mermaid 图表节点文字 ≤8 字，超 10 节点用 subgraph 分组
6. 单章 800-3000 字

### 论文援引规范

7. **充分援引原始论文**：每个核心概念首次出现时，必须引用提出该概念的原始论文，格式为「作者（年份）」内联引用 + 章末参考文献列表
8. **网络参考资料**：撰写前须从网络搜索代表性的介绍文档（教科书、技术博客、综述文章）作为参考，确保表述的准确性和通俗性达到平衡
9. **专业性与通俗性同等重要**：不能为了通俗而牺牲准确性，也不能为了严谨而丧失可读性。每一处表述须同时满足两个标准：(a) 专业人士审查无 factual error；(b) 零基础读者能理解核心含义
10. **参考来源分层**：原始论文 > 权威教科书（如 Jurafsky & Martin）> 高质量技术综述 > 技术博客。引用时优先使用高可靠性来源
11. **参考文献格式**：章末使用「## 参考文献」二级标题，按引用顺序编号列出，格式为 `[编号] 作者. 标题. 发表场所, 年份. 链接`

---

## 第一层：建立直觉

### 第 1 章 什么是语言模型

**文件**：`ch01-what-is-lm.md`  
**目标**：让读者理解语言模型的本质是"根据已有文字预测下一个词"  
**预估字数**：1500-2000 字

#### 章节结构

```
# 第 1 章 什么是语言模型

## 本章导引
> 语言模型是大语言模型的核心概念。本章从"预测下一个词"这一基本任务出发，
> 介绍语言模型的定义、概率链式分解的数学表达，以及从 n-gram 到神经语言模型
> 再到 Transformer 大模型的历史演进。

## 1.1 语言模型的定义
  - 语言模型的核心任务：给定一段文本的前缀，预测下一个词
  - 形式化表述：建模条件概率 P(w_t | w_1, w_2, ..., w_{t-1})
  - 自回归生成：逐词预测，每次将预测结果追加到输入中，循环生成完整文本
  - 引用 Shannon（1948）对语言统计建模的奠基性贡献

## 1.2 概率链式分解
  - 一个文本序列的联合概率可以分解为条件概率的连乘
  - 给出公式：P(w_1, w_2, ..., w_n) = ∏ P(w_t | w_1, ..., w_{t-1})
  - 逐符号解释：w_t 为第 t 个词，∏ 为连乘，P 为条件概率
  - 说明这一分解是自回归生成的数学基础

## 1.3 语言模型的历史演进
### 1.3.1 统计语言模型：n-gram
  - n-gram 模型的核心假设：下一个词只依赖前 n-1 个词（马尔可夫假设）
  - 引用 Shannon（1948）引入 n-gram 模型
  - n-gram 的局限：数据稀疏、上下文窗口短、无法捕捉语义相似性

### 1.3.2 神经语言模型
  - Bengio et al.（2003）提出神经概率语言模型，引入词的分布式表示
  - 词嵌入的概念萌芽：将离散的词映射为连续的稠密向量
  - 引用 Mikolov et al.（2013）Word2Vec，词向量技术的标志性工作
  - 神经网络语言模型解决了 n-gram 的数据稀疏问题

### 1.3.3 从 Transformer 到大模型时代
  - Vaswani et al.（2017）提出 Transformer 架构，引入自注意力机制
  - Radford et al.（2018）GPT 系列：基于 Transformer 的自回归语言模型
  - Brown et al.（2020）GPT-3：参数规模达到 175B，展示涌现能力
  - Kaplan et al.（2020）Scaling Laws：模型规模与性能的幂律关系
  - 当代大模型：参数规模从数十亿到数千亿，训练数据达万亿 token 级别

## 本章小结
  - 语言模型的核心任务是预测下一个词，形式化为条件概率
  - 概率链式分解将序列概率拆解为条件概率的连乘
  - 语言模型经历了 n-gram → 神经语言模型 → Transformer 大模型三个阶段
  - 当代大模型的规模和训练数据量达到了前所未有的量级
```

**内容约束**：

- 1.2 节的链式分解公式是全章唯一的数学公式，必须完整解释
- 1.3 节的演进是叙事主线，不要陷入 RNN/LSTM 的技术细节
- 1.3.3 节列举模型时只给数量级，不讨论具体模型差异（留给第 13 章）
- 不使用任何交互组件或 Mermaid 图表（本章以概念建立为主）
- 每个关键概念首次出现时须引用原始论文
- 参考文献列表至少包含：Shannon（1948）、Bengio et al.（2003）、Mikolov et al.（2013）、Vaswani et al.（2017）、Brown et al.（2020）、Kaplan et al.（2020）

---

### 第 2 章 Token 化

**文件**：`ch02-tokenization.md`  
**目标**：让读者理解文字如何被切分为模型处理的基本单元  
**预估字数**：1500-2000 字

#### 章节结构

```
# 第 2 章 Token 化

## 本章导引
> 大语言模型不直接处理文字，而是处理 Token。本章介绍 Token 化的过程、
> 常见的分词算法 BPE，以及中英文分词的差异。读者将通过交互演示直观感受分词过程。

## 2.1 为什么需要 Token 化
  - 模型只能处理数字，文字必须先转换为数字序列
  - 直接按字符切分：词表太小但序列太长
  - 直接按词切分：词表太大且无法处理新词
  - Token 是字符与词之间的折中单元

## 2.2 BPE：字节对编码算法
  - BPE 的核心思想：从字符开始，逐步合并最高频的相邻对
  - 用一个具体例子走完 BPE 合并过程（如 "low"/"lower"/"lowest"）
  - 说明 BPE 生成的词表大小是可配置的（如 50K、100K）
  - 给出 BPE 合并步骤的 Mermaid 流程图

## 2.3 特殊 Token
  - BOS/EOS/PAD 等特殊标记的作用
  - 说明不同模型使用不同的特殊 token 方案

## 2.4 中英文分词的差异
  - 英文天然以空格分词，BPE 在词级别合并
  - 中文无空格分隔，BPE 在字符级别合并
  - 同一段文字中英文的 token 数量比例约为 1:1.5~2（中文 token 更多）
  - 这影响了中文内容的上下文利用率

## 2.5 交互演示
  - 插入 <TokenDemo /> 组件
  - 引导读者输入文字，观察分词结果

## 本章小结
  - Token 化是将文字转换为模型可处理单元的过程
  - BPE 通过频次驱动的逐步合并，在字符和词之间取得平衡
  - 中英文分词机制不同，影响 token 效率
  - 词表大小是模型设计的重要参数
```

**内容约束**：

- BPE 的合并过程用一个 3-4 步的具体例子完整走一遍，不能只给抽象描述
- Mermaid 流程图展示 BPE 合并步骤（graph TD，约 6-8 节点）
- TokenDemo 组件插入在 2.5 节
- 不出现 BPE 的伪代码或代码实现，用文字+图解释

---

### 第 3 章 词嵌入

**文件**：`ch03-embedding.md`  
**目标**：让读者理解词语如何被表示为数值向量，以及向量空间中的语义关系  
**预估字数**：1800-2500 字

#### 章节结构

```
# 第 3 章 词嵌入

## 本章导引
> Token 化后，每个 Token 需要被映射为一个数值向量。本章介绍词嵌入的概念、
> 向量空间的语义特性，以及位置信息如何被编码。读者将通过可视化直观感受
> 词向量的语义聚类现象。

## 3.1 从离散到连续：为什么需要嵌入
  - One-hot 编码的问题：维度高、无语义关系、所有词正交
  - 嵌入的核心思想：用低维稠密向量表示词，让语义相近的词在空间中相近
  - 嵌入矩阵 E 的概念：词表大小 × 嵌入维度，查表操作得到词向量

## 3.2 向量空间中的语义关系
  - 语义相似性：词向量之间的距离（余弦相似度）反映语义接近程度
  - 给出余弦相似度公式：cos(a,b) = (a·b) / (|a||b|)
  - 解释每个符号：a·b 为点积，|a| 为向量模长
  - 经典示例：king - man + woman ≈ queen（词向量类比关系）
  - 插入 <WordEmbedViz /> 组件，展示词向量的二维降维投影

## 3.3 嵌入是如何学习到的
  - 嵌入向量不是人工设计的，而是在训练过程中自动学习
  - 核心原理：出现在相似上下文中的词，其嵌入向量趋于相近
  - 简要提及 Word2Vec / GloVe 的历史意义，但不深入
  - 在大模型中，嵌入是端到端训练的第一层

## 3.4 上下文相关嵌入
  - 静态嵌入（如 Word2Vec）的问题：同一个词在不同语境下含义不同
  - 大模型使用上下文相关的动态嵌入：同一个 token 在不同句子中的向量不同
  - 这为第 5 章注意力机制做铺垫

## 本章小结
  - 词嵌入将离散的 token 映射为连续的稠密向量
  - 语义相近的词在向量空间中距离更近
  - 余弦相似度是衡量向量语义相似性的常用指标
  - 大模型使用上下文相关的动态嵌入，而非静态嵌入
```

**内容约束**：

- 余弦相似度公式是本章唯一公式，必须解释每个符号
- WordEmbedViz 组件插入在 3.2 节
- king-man+woman≈queen 的例子必须给出，但需说明这是理想化示例
- 不展开 Word2Vec 的训练细节（SGNS 等），只说明"上下文相似→向量相近"的原理

---

### 第 4 章 Transformer 架构概览

**文件**：`ch04-transformer.md`  
**目标**：让读者理解 Transformer 的整体架构和数据流，不深入细节  
**预估字数**：1800-2500 字

#### 章节结构

```
# 第 4 章 Transformer 架构概览

## 本章导引
> Transformer 是大语言模型的核心架构。本章从整体视角介绍其编码器-解码器结构、
> GPT 类模型的解码器架构，以及数据在模型中的完整流转过程。注意力机制等细节
> 留待第 5 章展开。

## 4.1 Transformer 的诞生背景
  - Transformer 之前的序列模型（RNN/LSTM）的局限：顺序计算慢、长距离依赖弱
  - Transformer（2017, Attention Is All You Need）的核心创新：完全基于注意力，无循环结构
  - 并行计算优势：所有位置同时处理

## 4.2 编码器-解码器结构
  - 原始 Transformer 的双塔结构
  - 编码器：将输入序列编码为上下文表示
  - 解码器：基于编码结果逐步生成输出
  - 插入 Mermaid 架构图（编码器-解码器整体结构）

## 4.3 GPT 类模型：仅解码器架构
  - GPT 系列只使用解码器部分
  - 原因：自回归生成任务天然适合解码器结构
  - 解码器的核心组件：掩码自注意力 + 前馈网络 + 残差连接 + 层归一化
  - 插入 Mermaid 架构图（单个解码器层的内部结构）

## 4.4 数据流：从 Token 到输出概率
  - 完整数据流走查：
    1. 输入 token 序列 → 嵌入层 → 加位置编码
    2. 经过 N 个解码器层（N 通常为 6~96 层）
    3. 最终层输出 → 线性变换 → softmax → 下一个 token 的概率分布
  - 插入 Mermaid 数据流图（从输入到输出的完整流程）

## 4.5 关键超参数
  - 层数（N）、隐藏维度（d_model）、注意力头数（h）、词表大小（V）
  - 用表格列举几个主流模型的超参数量级（GPT-2 small / GPT-3 / Llama 等）
  - 说明这些参数如何影响模型能力和计算成本

## 本章小结
  - Transformer 摒弃了循环结构，完全基于注意力机制
  - GPT 类大模型采用仅解码器架构，通过自回归方式生成文本
  - 数据流：Token → 嵌入 → 解码器层堆叠 → softmax → 概率分布
  - 层数、隐藏维度、注意力头数是决定模型规模的核心超参数
```

**内容约束**：

- 本章插入 3 个 Mermaid 图：编码器-解码器整体结构、单个解码器层内部结构、完整数据流
- 不展开注意力机制的数学细节（留给第 5 章），只说明"解码器层包含掩码自注意力"
- 残差连接和层归一化只一句话提及，不展开解释
- 超参数表格给量级（如"96 层"），不给精确架构图

---

### 第 5 章 注意力机制

**文件**：`ch05-attention.md`  
**目标**：让读者理解自注意力机制的工作原理，这是大模型的核心  
**预估字数**：2000-2800 字

#### 章节结构

```
# 第 5 章 注意力机制

## 本章导引
> 注意力机制是 Transformer 的核心创新。本章介绍自注意力的工作原理、
> Q/K/V 三个矩阵的角色、缩放点积注意力的计算方式，以及多头注意力机制。
> 本章包含全文档最关键的可视化演示。

## 5.1 注意力机制的核心思想
  - 直觉：阅读句子时，不同词之间的关联程度不同
  - 例句："猫坐在垫子上，它很暖和"——"它"应该关注"垫子"而非"猫"
  - 注意力机制让模型自动学习词与词之间的关联权重

## 5.2 查询、键、值：Q/K/V
  - 三个线性变换矩阵 WQ, WK, WV 将输入向量映射为 Q, K, V
  - 类比（辅助）：图书馆检索——Q 是查询请求，K 是书名标签，V 是书籍内容
  - 类比后回到严谨表述：Q/K/V 均由输入向量经线性变换得到
  - 插入 Mermaid 图展示 Q/K/V 的生成过程

## 5.3 缩放点积注意力
  - 核心公式：Attention(Q,K,V) = softmax(QK^T / √dk) V
  - 逐部分解释：
    - QK^T：查询与键的点积，衡量相关性
    - √dk：缩放因子，防止点积值过大导致 softmax 梯度消失
    - softmax：将相关性分数归一化为概率分布
    - 乘以 V：按权重加权求和得到输出
  - dk 的含义：键向量的维度
  - 插入 <AttentionViz /> 组件，展示注意力权重热力图

## 5.4 多头注意力
  - 单一注意力可能只能捕捉一种关系
  - 多头注意力：将 Q/K/V 分成 h 组，各自独立计算注意力，最后拼接
  - 不同头可以关注不同类型的关系（语法、语义、指代等）
  - 插入 Mermaid 图展示多头注意力的并行结构

## 5.5 掩码自注意力
  - GPT 类模型中，生成时不能看到未来的 token
  - 掩码操作：将注意力矩阵的上三角部分设为 -∞（softmax 后为 0）
  - 这保证自回归生成的因果性

## 本章小结
  - 注意力机制通过 Q/K/V 三个矩阵计算词与词之间的关联权重
  - 缩放点积公式：softmax(QK^T/√dk)V 是注意力机制的核心运算
  - 多头注意力允许模型同时捕捉多种语义关系
  - 掩码自注意力保证了自回归生成的因果性
```

**内容约束**：

- 本章是全文档核心，公式最密集，但每个公式必须配文字解释
- 5.3 节的缩放点积公式是全文档最重要的公式，必须逐部分拆解
- Mermaid 图 × 2（Q/K/V 生成过程 + 多头并行结构）
- AttentionViz 组件插入在 5.3 节公式解释之后
- 图书馆检索类比是本章唯一类比，使用后立即回到严谨表述
- 掩码部分不展开矩阵运算细节，用文字+一句话说明即可

---

### 第 6 章 训练过程

**文件**：`ch06-training.md`  
**目标**：让读者理解大模型是如何被"训练"出来的，包括预训练和微调两个阶段  
**预估字数**：2000-2800 字

#### 章节结构

```
# 第 6 章 训练过程

## 本章导引
> 大模型的能力并非天生，而是通过大规模训练获得。本章介绍预训练、监督微调（SFT）
> 和基于人类反馈的强化学习（RLHF）三个训练阶段，以及损失函数的基本概念。

## 6.1 训练的总体流程
  - 三阶段训练范式：预训练 → SFT → RLHF
  - 插入 Mermaid 流程图（三阶段训练流程）

## 6.2 预训练：从海量文本中学习语言规律
  - 训练目标：Next Token Prediction（预测下一个 token）
  - 训练数据：互联网文本、书籍、代码等（TB 级）
  - 损失函数：交叉熵损失
  - 给出交叉熵损失公式：L = -∑ wi log P(wi | w1,...,wi-1)
  - 解释符号：wi 是真实 token，P 是模型预测概率，∑ 对序列所有位置求和
  - 说明预训练后的模型能做"续写"但还不会"对话"

## 6.3 监督微调（SFT）：学会遵循指令
  - 预训练模型只会续写文本，不理解"指令"概念
  - SFT 使用高质量的"指令-回复"对进行微调
  - 训练目标不变（仍是 Next Token Prediction），但数据格式变了
  - SFT 后的模型能理解"请帮我写一封邮件"这类指令

## 6.4 RLHF：对齐人类偏好
  - SFT 的局限：模型回复可能不符合人类偏好（冗长、不安全、偏题等）
  - RLHF 三步骤：
    1. 训练奖励模型（RM）：用人类标注的偏好数据训练打分模型
    2. 强化学习优化：用 PPO 算法最大化奖励模型的打分
    3. 防止过度偏离：加入 KL 散度约束，限制模型不偏离 SFT 版本太远
  - 简要提及 DPO 作为 RLHF 的简化替代方案
  - 不展开 PPO 算法细节

## 6.5 训练的工程现实
  - 训练成本：GPT-3 级别模型训练成本约数百万美元
  - 数据质量比数据量更重要
  - 训练不稳定：损失函数可能出现突发性跳变
  - 这些工程现实解释了为什么大模型训练门槛极高

## 本章小结
  - 大模型训练分三阶段：预训练 → SFT → RLHF
  - 预训练通过 Next Token Prediction 从海量文本中学习语言规律
  - 交叉熵损失是预训练的核心目标函数
  - SFT 让模型学会遵循指令，RLHF 让模型回复对齐人类偏好
  - 训练成本和数据质量是决定模型能力的关键工程因素
```

**内容约束**：

- 交叉熵损失公式是本章核心公式，必须解释每个符号
- Mermaid 图 × 1（三阶段训练流程）
- RLHF 三步骤用编号列表清晰呈现，不展开 PPO 数学推导
- DPO 只一句话提及，不展开
- 训练成本给量级（"数百万美元"），不给精确数字

---

### 第 7 章 涌现能力

**文件**：`ch07-emergence.md`  
**目标**：让读者理解模型规模增大后为何会出现"涌现"能力，以及这一概念的定义与争议  
**预估字数**：1200-1800 字

#### 章节结构

```
# 第 7 章 涌现能力

## 本章导引
> 大语言模型最引人注目的特性之一是"涌现能力"——当模型规模增大到一定程度时，
> 某些能力会突然出现。本章介绍涌现现象的定义、典型案例和学术争议。

## 7.1 什么是涌现能力
  - 定义：在模型规模（参数量、数据量、计算量）较小时不存在，但规模增大到某个阈值后突然出现的能力
  - 关键特征：能力的提升不是渐进的，而是突变的
  - 类比（辅助）：水的相变——温度从 99°C 到 100°C，液态突然变为气态
  - 类比后回到严谨表述

## 7.2 涌现能力的典型案例
  - 少样本学习（Few-shot Learning）：小模型无法从几个例子中学习，大模型可以
  - 思维链（Chain of Thought）：小模型直接回答效果差，大模型通过"逐步推理"大幅提升
  - 指令遵循：小模型只会续写，大模型能理解并执行复杂指令
  - 用表格对比不同规模模型在这些任务上的表现差异

## 7.3 涌现的成因与争议
  - 乐观观点：涌现暗示大模型可能发展出超越训练任务的能力
  - 谨慎观点：
    - 评估指标可能是非线性的，能力本身是渐进的，只是指标突变
    - "涌现"可能只是评估方式的假象
  - 当前学术界对涌现的本质尚无定论

## 7.4 对实践者的启示
  - 模型规模不是万能的：小模型在特定垂类任务上可能更高效
  - 理解模型的能力边界比追求规模更重要（为第二层铺垫）

## 本章小结
  - 涌现能力是指模型规模超过阈值后突然出现的全新能力
  - 少样本学习、思维链推理是典型涌现现象
  - 涌现的本质（真实突变 vs 评估假象）尚有学术争议
  - 实践中应根据任务需求选择模型规模，而非盲目追求大模型
```

**内容约束**：

- 本章无公式、无交互组件、无 Mermaid 图
- 水的相变类比是本章唯一类比
- 能力对比表格给定性描述（"无法"/"显著提升"），不给精确分数
- 7.3 节必须呈现正反两面观点，保持客观

---

## 第二层：理解边界

### 第 8 章 上下文窗口

**文件**：`ch08-context-window.md`  
**目标**：让读者理解上下文窗口的含义、限制来源和位置编码的作用  
**预估字数**：1500-2000 字

#### 章节结构

```
# 第 8 章 上下文窗口

## 本章导引
> 大模型能处理的文本长度存在上限，这个上限称为"上下文窗口"。本章介绍上下文窗口的
> 来源、位置编码的作用，以及长文本处理的常见策略。

## 8.1 上下文窗口的定义
  - 定义：模型在一次推理中能处理的最大 token 数量
  - 上下文窗口包含：输入（prompt）+ 输出（generation）
  - 不同模型的窗口大小：4K → 8K → 32K → 128K → 1M 的演进
  - 窗口大小对使用场景的影响

## 8.2 窗口限制的来源
  - 注意力机制的计算复杂度：O(n²)——序列长度翻倍，计算量变四倍
  - 显存占用：KV Cache 随序列长度线性增长
  - 这解释了为什么扩大上下文窗口在工程上极具挑战

## 8.3 位置编码
  - 注意力机制本身是排列不变的（permutation invariant），需要额外注入位置信息
  - 绝对位置编码 vs 相对位置编码
  - RoPE（旋转位置编码）的概念：
    - 通过旋转变换将位置信息编码到 Q/K 向量中
    - 给出 RoPE 的核心公式（旋转矩阵形式），配文字解释
  - RoPE 的优势：支持相对距离、外推性好

## 8.4 长文本处理策略
  - 滑动窗口：只保留最近的 N 个 token
  - 摘要压缩：将历史对话摘要后放入上下文
  - RAG：从外部检索相关片段（为第 15 章铺垫）
  - 稀疏注意力：减少注意力计算的复杂度

## 本章小结
  - 上下文窗口是模型单次推理能处理的最大 token 数
  - 限制来源于注意力机制的 O(n²) 计算复杂度和 KV Cache 的显存占用
  - 位置编码为排列不变的注意力机制注入位置信息
  - RoPE 是当前主流的相对位置编码方案
  - 长文本处理有多种策略，各有权衡
```

**内容约束**：

- RoPE 公式给出旋转矩阵形式即可，不展开推导
- 注意力复杂度 O(n²) 用文字解释清楚"翻倍变四倍"
- 不展开稀疏注意力的具体算法细节

---

### 第 9 章 幻觉问题

**文件**：`ch09-hallucination.md`  
**目标**：让读者理解幻觉的成因和缓解方式  
**预估字数**：1200-1800 字

#### 章节结构

```
# 第 9 章 幻觉问题

## 本章导引
> 大模型会生成看似合理但实际错误的内容，这被称为"幻觉"。本章分析幻觉的成因，
> 解释其与模型本质的关系，并介绍常见的缓解策略。

## 9.1 什么是幻觉
  - 定义：模型生成的内容在语法上流畅、在语义上合理，但事实错误或逻辑错误
  - 典型案例：编造不存在的论文引用、虚构历史事件细节
  - 幻觉与"说谎"的区别：模型没有欺骗意图，它是真的"认为"生成的内容是对的

## 9.2 幻觉的成因
  - 根本原因：大模型是概率生成模型，不是知识检索系统
  - 训练目标决定行为：Next Token Prediction 选择"最可能"的下一个词，而非"最正确"的
  - 训练数据中的噪声和矛盾
  - 模型不知道自己"不知道什么"（知识边界不可知）

## 9.3 幻觉的类型
  - 事实性幻觉：生成与客观事实不符的内容
  - 忠实性幻觉：生成的内容与给定输入/上下文矛盾
  - 两种类型的区别和联系

## 9.4 缓解策略
  - RAG：让模型基于检索到的事实生成（为第 15 章铺垫）
  - 提示工程：要求模型标注不确定性、分步推理
  - 温度参数：降低温度减少随机性
  - 事后验证：外部知识库校验
  - 强调：目前没有完全消除幻觉的方法

## 本章小结
  - 幻觉是大模型生成看似合理但实际错误内容的现象
  - 根本原因是大模型的概率生成本质，而非 bug
  - 事实性幻觉和忠实性幻觉是两种主要类型
  - RAG、提示工程、温度调节可缓解但无法完全消除幻觉
```

**内容约束**：

- 本章无公式、无交互组件、无 Mermaid 图
- 案例用具体例子（编造论文引用等），不用抽象描述
- "模型不知道自己不知道什么"这句话要点明，是核心理解
- 缓解策略不展开技术实现，只介绍概念

---

### 第 10 章 能力边界

**文件**：`ch10-capability-boundary.md`  
**目标**：让读者清楚大模型擅长什么、不擅长什么，以及为什么  
**预估字数**：1500-2000 字

#### 章节结构

```
# 第 10 章 能力边界

## 本章导引
> 理解大模型不能做什么，与理解它能做什么同样重要。本章系统梳理大模型的
> 能力边界，分析其擅长的任务类型和固有的短板，帮助读者建立合理的预期。

## 10.1 大模型擅长的任务
  - 文本生成与改写：续写、摘要、风格转换
  - 信息抽取：从非结构化文本中提取结构化信息
  - 代码生成：常见语言的代码编写和调试
  - 语言理解：语义分析、情感判断、文本分类
  - 知识问答：基于训练数据中的知识回答问题

## 10.2 大模型的短板
  - 精确数学计算：大模型不擅长需要精确计算的任务（本质是概率模型而非计算器）
  - 严格逻辑推理：多步演绎推理容易出错
  - 实时信息：训练数据有截止日期，无法获取训练后的信息
  - 可靠性要求高的任务：医疗诊断、法律判断等需要确定性输出的场景
  - 长程规划：复杂多步骤任务的规划能力有限

## 10.3 为什么会有这些边界
  - 概率模型的本质：选择"最可能"而非"最正确"的输出
  - 训练数据的局限：数据覆盖范围和时效性
  - 上下文窗口限制：长程依赖受限于窗口大小
  - 缺乏外部工具调用能力（原生模型）：模型被困在语言空间中

## 10.4 能力雷达图
  - 插入 Mermaid 图或 SVG 雷达图，展示大模型在各任务维度上的能力分布
  - 维度：文本生成、代码生成、知识问答、数学计算、逻辑推理、实时信息、长程规划

## 本章小结
  - 大模型擅长文本相关任务和模式识别类任务
  - 精确计算、严格推理、实时信息、长程规划是固有短板
  - 这些边界源于模型的概率生成本质、训练数据局限和架构约束
  - 理解边界有助于合理使用模型，避免在错误场景中期望过高
```

**内容约束**：

- 能力雷达图用 Mermaid 或表格呈现，不给精确分数，用"强/中/弱"定性标注
- 10.3 节的"为什么"分析是本章重点，不能只列短板不解释原因
- 不讨论具体的 benchmark 分数

---

### 第 11 章 提示工程基础

**文件**：`ch11-prompt-engineering.md`  
**目标**：让读者掌握基本的提示工程技巧，更有效地使用大模型  
**预估字数**：1500-2000 字

#### 章节结构

```
# 第 11 章 提示工程基础

## 本章导引
> 提示工程是通过设计输入文本来引导大模型生成更优输出的方法。本章介绍
> 基本的提示策略，包括零样本、少样本和系统提示。

## 11.1 什么是提示工程
  - 定义：通过设计和优化输入提示来改善模型输出质量的方法
  - 提示工程不是"骗"模型，而是利用模型已有的能力

## 11.2 零样本与少样本
  - Zero-shot：不给示例，直接描述任务
  - Few-shot：给几个输入-输出示例，让模型学习模式
  - 代码块示例：展示同一任务用 zero-shot 和 few-shot 的不同提示写法
  - Few-shot 的示例数量和顺序都会影响效果

## 11.3 提示的结构化设计
  - 角色设定：给模型一个明确的角色（"你是一位专业的医学翻译"）
  - 任务描述：清晰说明要做什么
  - 输出格式：指定输出格式（JSON、表格、列表等）
  - 约束条件：说明限制（"不要超过 200 字"）
  - 代码块示例：一个结构化的提示模板

## 11.4 思维链提示
  - Chain of Thought (CoT)：让模型"逐步思考"再给答案
  - 在提示中加入"让我们一步一步来思考"
  - 代码块示例：CoT 提示的写法
  - CoT 对推理类任务的提升显著，对简单任务效果不明显

## 11.5 常见的提示陷阱
  - 指令冲突：多个矛盾的约束导致模型困惑
  - 过度示例：示例太多占用上下文窗口
  - 模糊描述：任务描述不明确导致输出偏离预期

## 本章小结
  - 提示工程通过设计输入文本来优化模型输出
  - Zero-shot 直接描述任务，Few-shot 提供示例引导模式
  - 结构化提示包含角色、任务、格式、约束四个要素
  - 思维链提示对推理类任务有显著提升
```

**内容约束**：

- 本章有 3-4 个代码块示例（提示文本），使用 `text 或 `markdown 标记
- 不使用任何 Mermaid 图或交互组件
- 示例提示必须是完整可用的，不是占位符

---

### 第 12 章 模型评估

**文件**：`ch12-evaluation.md`  
**目标**：让读者了解如何评判一个大模型的好坏，以及评估的局限性  
**预估字数**：1200-1800 字

#### 章节结构

```
# 第 12 章 模型评估

## 本章导引
> 如何判断一个大模型"好不好"？本章介绍常见的评估指标和评估方法，
> 以及当前评估体系面临的挑战。

## 12.1 困惑度（Perplexity）
  - 定义：衡量语言模型对文本的预测能力，困惑度越低越好
  - 给出困惑度公式：PPL = exp(-1/N ∑ log P(wi | context))
  - 解释符号：N 为序列长度，P 为模型预测概率
  - 局限性：困惑度衡量的是语言建模能力，不直接反映任务能力

## 12.2 Benchmark 评估
  - MMLU：多领域知识问答（57 个学科）
  - HumanEval：代码生成能力
  - GSM8K：小学数学推理
  - 说明 Benchmark 评估的方式：在固定测试集上运行，计算准确率
  - 用表格列举几个主流 Benchmark 及其评估维度

## 12.3 人类评估与竞技场
  - LMSYS Chatbot Arena：人类盲测投票排名
  - Elo 评分系统的基本概念
  - 人类评估的优势：更接近真实使用体验
  - 人类评估的局限：成本高、主观性强

## 12.4 评估的挑战
  - 数据污染：测试集可能泄露到训练数据中
  - Benchmark 过拟合：模型针对测试集优化而非真实能力提升
  - 评估维度有限：创造力、安全性等难以量化
  - 评估速度跟不上模型迭代速度

## 本章小结
  - 困惑度衡量语言建模能力，但不直接反映任务表现
  - Benchmark（MMLU、HumanEval 等）提供标准化的能力评估
  - 人类评估（如 Chatbot Arena）更贴近真实体验但成本高
  - 数据污染和 Benchmark 过拟合是当前评估体系的主要挑战
```

**内容约束**：

- 困惑度公式是本章唯一公式，必须解释符号
- Benchmark 表格给名称+评估维度+一句话说明
- 不讨论具体的评分细节和排名

---

## 第三层：走向实践

### 第 13 章 开源与闭源生态

**文件**：`ch13-ecosystem.md`  
**目标**：让读者了解当前大模型的生态格局，开源与闭源的权衡  
**预估字数**：1500-2000 字

#### 章节结构

```
# 第 13 章 开源与闭源生态

## 本章导引
> 当前大语言模型生态分为开源与闭源两大阵营。本章梳理主流模型谱系，
> 分析两种路线的权衡，帮助读者建立生态全景认知。

## 13.1 闭源模型阵营
  - 代表模型：GPT 系列（OpenAI）、Claude 系列（Anthropic）、Gemini 系列（Google）
  - 特点：通过 API 提供服务，模型权重不公开
  - 优势：性能领先、使用简单、持续迭代
  - 劣势：数据隐私风险、API 依赖、成本不可控

## 13.2 开源模型阵营
  - 代表模型：Llama 系列（Meta）、Qwen 系列（阿里）、DeepSeek 系列、Mistral 系列
  - 特点：模型权重公开下载，可本地部署
  - 优势：数据隐私可控、可定制、长期成本可控
  - 劣势：性能可能落后闭源、部署需要硬件资源、需自行维护

## 13.3 开源 vs 闭源的权衡矩阵
  - 用表格从多个维度对比：
    - 性能、成本（短期/长期）、隐私、定制性、部署难度、迭代速度
  - 插入 Mermaid 决策流程图：如何根据需求选择开源或闭源

## 13.4 模型选择的初步考量
  - 任务类型：通用对话 vs 垂类专业任务
  - 数据敏感度：能否将数据发给第三方 API
  - 预算：API 调用费用 vs 本地部署硬件成本
  - 技术能力：团队是否有部署和运维能力

## 本章小结
  - 闭源模型性能领先但依赖 API，开源模型可本地部署但需硬件投入
  - 主流开源模型已具备较强的通用能力，在垂类场景中可能足够
  - 选择开源还是闭源取决于任务需求、数据敏感度、预算和技术能力
```

**内容约束**：

- 模型名称列举截至文档撰写时的主流模型，不预测未来
- Mermaid 决策流程图展示选型逻辑（约 8-10 节点）
- 不评价哪个模型"最好"，只做客观对比

---

### 第 14 章 模型选择策略

**文件**：`ch14-model-selection.md`  
**目标**：提供具体的模型选型方法论  
**预估字数**：1200-1800 字

#### 章节结构

```
# 第 14 章 模型选择策略

## 本章导引
> 面对众多大模型，如何为具体场景选择合适的模型？本章提供一套选型方法论，
> 从任务需求、性能、成本三个维度展开分析。

## 14.1 选型的核心维度
  - 任务类型：生成、理解、推理、编码——不同模型各有强弱
  - 参数量：7B / 13B / 70B / 400B+，参数量影响性能和部署成本
  - 上下文窗口：是否需要处理长文本
  - 语言能力：中文、英文、多语言支持
  - 许可证：商业使用是否受限

## 14.2 参数量与性能/成本的权衡
  - 参数量与能力的关系：不是线性增长，存在边际递减
  - 参数量与推理成本：显存占用、推理速度
  - 用表格对比不同参数量级的部署需求（GPU 显存、推理速度）
  - 插入 Mermaid 图：参数量-性能-成本的三维权衡示意

## 14.3 选型决策流程
  - Step 1：明确任务类型和性能要求
  - Step 2：确定是否需要本地部署（数据隐私）
  - Step 3：根据预算筛选参数量范围
  - Step 4：在候选模型上做小规模评测
  - 插入 Mermaid 决策流程图

## 14.4 常见场景的推荐方案
  - 通用对话助手：闭源 API 或 70B+ 开源模型
  - 垂类专业任务：7B-13B 开源模型 + 微调
  - 长文本处理：选择大上下文窗口模型
  - 边缘设备部署：1B-7B 量化模型
  - 用表格列举场景与推荐方案

## 本章小结
  - 模型选型从任务类型、参数量、上下文窗口、语言能力、许可证五个维度考量
  - 参数量增长带来的性能提升存在边际递减
  - 选型应遵循"明确需求 → 确定部署方式 → 筛选参数量 → 实测验证"的流程
```

**内容约束**：

- 两个表格（参数量部署需求 + 场景推荐方案）
- 两个 Mermaid 图（三维权衡示意 + 决策流程图）
- 不推荐具体型号，只推荐参数量级和方案类型

---

### 第 15 章 RAG 检索增强生成

**文件**：`ch15-rag.md`  
**目标**：让读者理解 RAG 的原理、价值以及与微调的对比  
**预估字数**：1800-2500 字

#### 章节结构

```
# 第 15 章 RAG 检索增强生成

## 本章导引
> RAG（检索增强生成）是解决大模型知识时效性和幻觉问题的重要方案。
> 本章介绍 RAG 的原理、架构、关键组件，并与微调进行对比。

## 15.1 为什么需要 RAG
  - 大模型的固有局限：知识有截止日期、无法访问私有数据、容易产生幻觉
  - RAG 的核心思想：在生成前先从外部知识库检索相关信息，将其作为上下文提供给模型
  - RAG = 检索 + 生成

## 15.2 RAG 的架构
  - 完整 RAG 流程：
    1. 文档预处理：分块（chunking）
    2. 向量化：用嵌入模型将文本块编码为向量
    3. 存储：将向量存入向量数据库
    4. 检索：用户查询向量化后，在向量数据库中检索最相似的文本块
    5. 生成：将检索到的文本块与用户查询一起送入大模型生成回答
  - 插入 Mermaid 架构图（完整 RAG 流程）

## 15.3 RAG 的关键组件
### 15.3.1 嵌入模型
  - 作用：将文本转换为向量
  - 选型考量：语言支持、维度、速度
  - 常见模型：BGE、E5、text-embedding 系列

### 15.3.2 向量数据库
  - 作用：存储和高效检索向量
  - 常见方案：Faiss、Milvus、Chroma、Qdrant
  - 选型考量：规模、性能、易用性

### 15.3.3 检索策略
  - 稠密检索：基于向量相似度
  - 稀疏检索：基于关键词匹配（BM25）
  - 混合检索：两者结合

## 15.4 RAG vs 微调
  - 用表格对比 RAG 和微调：
    - 知识更新、成本、效果、适用场景
  - RAG 适合：知识频繁更新、需要引用来源、私有数据
  - 微调适合：改变模型行为模式、特定领域术语、输出格式
  - 两者可以结合使用

## 本章小结
  - RAG 通过"先检索后生成"的方式弥补大模型的知识局限
  - 完整流程：分块 → 向量化 → 存储 → 检索 → 生成
  - 嵌入模型、向量数据库、检索策略是三大关键组件
  - RAG 与微调不是替代关系，而是互补关系
```

**内容约束**：

- Mermaid 图 × 1（RAG 完整流程架构图，约 8-10 节点，使用 subgraph 分组）
- RAG vs 微调对比表格从 4 个维度对比
- 不展开 Faiss/Milvus 等的具体使用方法
- 检索策略只介绍概念，不展开算法细节

---

### 第 16 章 微调入门

**文件**：`ch16-finetuning.md`  
**目标**：让读者理解微调的基本概念、LoRA 的原理和适用场景  
**预估字数**：1500-2000 字

#### 章节结构

```
# 第 16 章 微调入门

## 本章导引
> 微调是在已有大模型基础上，用特定领域数据进一步训练以适配具体任务的方法。
> 本章介绍全量微调与参数高效微调（PEFT）的区别，重点讲解 LoRA 的原理。

## 16.1 什么是微调
  - 定义：在预训练模型的基础上，用领域特定数据进行进一步训练
  - 微调改变的是模型参数（或部分参数），而非外部知识
  - 微调 vs RAG：微调改变模型"怎么回答"，RAG 改变模型"知道什么"

## 16.2 全量微调
  - 做法：更新模型所有参数
  - 优势：效果上限高
  - 劣势：训练成本高、显存需求大、容易过拟合、存储成本高（每个微调版本一份完整模型）

## 16.3 参数高效微调（PEFT）
  - 核心思想：冻结大部分参数，只训练少量新增参数
  - 优势：训练成本低、显存需求小、可插拔
  - 代表方法：LoRA、Prefix Tuning、P-Tuning

## 16.4 LoRA 详解
  - LoRA（Low-Rank Adaptation）的核心思想：
    - 不直接训练原始权重矩阵 W，而是训练一个低秩增量 ΔW = BA
    - 其中 B（d×r）和 A（r×d）是两个小矩阵，r 远小于 d
    - 前向传播：Wx + BAx = (W + BA)x
  - 给出 LoRA 的数学表达：h = Wx + BAx
  - 解释符号：W 为冻结的原始权重，B/A 为可训练的低秩矩阵，x 为输入
  - LoRA 的优势：参数量从 d² 降到 2dr，r 通常取 8-64
  - 插入 Mermaid 图展示 LoRA 的结构

## 16.5 QLoRA
  - 在 LoRA 基础上进一步将原始权重量化为 4-bit
  - 大幅降低显存需求：可在单张消费级 GPU 上微调 70B 模型
  - 代价：训练速度有所降低

## 16.6 微调的数据准备
  - 数据格式：指令-回复对（与 SFT 类似）
  - 数据质量比数量更重要：500 条高质量数据可能优于 5000 条低质量数据
  - 数据多样性：覆盖任务的多种表达方式

## 本章小结
  - 微调在预训练模型基础上用领域数据进一步训练
  - 全量微调效果上限高但成本大，PEFT 成本低且可插拔
  - LoRA 通过低秩矩阵分解将可训练参数量降低数个数量级
  - QLoRA 结合量化进一步降低显存门槛
  - 数据质量是微调效果的关键因素
```

**内容约束**：

- LoRA 公式 h = Wx + BAx 是本章核心，必须解释所有符号
- Mermaid 图 × 1（LoRA 结构示意）
- 不展开 LoRA 的训练超参数调优细节
- 不提供微调代码实现，只讲原理

---

### 第 17 章 部署方式概览

**文件**：`ch17-deployment.md`  
**目标**：让读者了解大模型的部署方式选型  
**预估字数**：1200-1800 字

#### 章节结构

```
# 第 17 章 部署方式概览

## 本章导引
> 选择部署方式是大模型落地的重要决策。本章介绍三种主流部署方式及其适用场景。

## 17.1 API 调用
  - 方式：通过闭源模型厂商的 API 接口调用
  - 优势：零运维、弹性扩缩、始终使用最新模型
  - 劣势：数据隐私风险、API 费用、网络延迟、服务商依赖
  - 适用场景：快速验证、对数据隐私要求不高、流量波动大

## 17.2 云端托管
  - 方式：在云服务器上部署开源模型（如 AWS EC2、阿里云 ECS）
  - 优势：数据可控、模型可选、弹性扩展
  - 劣势：需要运维能力、GPU 实例成本、部署复杂度
  - 适用场景：中等规模使用、有运维团队、需要数据隐私

## 17.3 本地部署
  - 方式：在本地服务器或工作站上部署模型
  - 优势：完全数据控制、无网络依赖、长期成本可控
  - 劣势：硬件投入大、运维复杂、扩展性差
  - 适用场景：高数据安全要求、固定规模使用、有硬件资源

## 17.4 量化部署
  - 量化的概念：将模型参数从 FP16 降为 INT8/INT4，减少显存占用
  - 常见量化方案：GGUF、AWQ、GPTQ
  - 量化代价：精度损失，但通常在可接受范围
  - 量化使大模型能在消费级硬件上运行

## 17.5 推理框架
  - vLLM：高吞吐量推理引擎，支持 PagedAttention
  - Ollama：简化本地部署，适合个人使用
  - TensorRT-LLM：NVIDIA 官方优化推理引擎
  - 用表格对比三个框架的特点和适用场景

## 本章小结
  - API 调用适合快速验证，云端托管适合中等规模，本地部署适合高安全要求
  - 量化部署通过降低参数精度大幅减少硬件需求
  - vLLM、Ollama、TensorRT-LLM 是主流推理框架，各有适用场景
```

**内容约束**：

- 三种部署方式用表格对比
- 推理框架对比表格给特点+适用场景
- 不展开量化算法细节
- 不提供具体的部署操作步骤

---

### 第 18 章 成本与性能考量

**文件**：`ch18-cost-performance.md`  
**目标**：让读者理解大模型使用和部署的成本结构，建立性能与成本的权衡意识  
**预估字数**：1200-1800 字

#### 章节结构

```
# 第 18 章 成本与性能考量

## 本章导引
> 大模型的实际应用不仅是技术问题，更是成本与性能的权衡问题。本章梳理
> 大模型使用和部署的成本结构，帮助读者建立经济可行的方案。

## 18.1 API 调用的成本结构
  - 计费方式：按 token 计费（输入 token + 输出 token）
  - 价格量级：不同模型的 token 单价差异可达 10 倍以上
  - 隐性成本：上下文冗余、重试、缓存失效
  - 用表格列举几个主流模型的 API 价格量级（不给精确数字，给量级范围）

## 18.2 本地部署的成本结构
  - 硬件成本：GPU 购买/租赁
  - 运维成本：电力、网络、人力
  - 利用率：GPU 利用率通常远低于 100%，闲置成本
  - 用表格对比不同参数量模型的硬件需求量级

## 18.3 性能指标
  - 延迟（Latency）：首 token 延迟（TTFT）+ 生成速度（tokens/s）
  - 吞吐量（Throughput）：单位时间处理的请求数
  - 延迟与吞吐量的权衡：Batch size 增大提高吞吐量但增加延迟
  - 并发用户数与体验的关系

## 18.4 成本优化策略
  - 模型路由：简单任务用小模型，复杂任务用大模型
  - 缓存：对相同查询缓存结果
  - 量化：降低推理成本
  - 上下文压缩：减少输入 token 数
  - 批处理：提高 GPU 利用率

## 18.5 决策框架
  - 插入 Mermaid 决策流程图：从需求到方案的成本-性能决策流程
  - 核心问题：月调用量、延迟要求、数据隐私、预算上限

## 本章小结
  - API 成本按 token 计费，不同模型价格差异大
  - 本地部署的硬件和运维成本需考虑 GPU 利用率
  - 延迟和吞吐量是核心性能指标，两者存在权衡
  - 模型路由、缓存、量化是主要的成本优化手段
```

**内容约束**：

- 两个表格（API 价格量级 + 硬件需求量级），只给量级范围不给精确数字
- Mermaid 图 × 1（成本-性能决策流程）
- 不讨论具体的云厂商定价方案
- 性能指标用文字解释清楚 TTFT 和 tokens/s 的含义

---

## 审核流程

1. 本文档列出全部 18 章的详细内容规格
2. 用户逐章审核，可修改结构、增删小节、调整约束
3. 审核通过后，按章节顺序撰写实际 `.md` 文件
4. 每章撰写完成后，用户确认后再进入下一章
